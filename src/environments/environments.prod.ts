export const environment = {
  production: true,
  apiUrl: 'https://www.yourdomain.com/api',
  debug: false
};

export const auth = {
  production: true,
  apiUrl: 'https://www.yourdomain.com/auth',
  debug: true
};
