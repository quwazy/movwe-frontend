export const environment = {
  production: true,
  apiUrl: 'https://api.yourdomain.com',
  debug: false
};

export const auth = {
  production: true,
  apiUrl: 'http://api.yourdomain.com/auth',
  debug: true
};
